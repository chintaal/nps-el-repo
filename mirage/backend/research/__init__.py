"""Research subsystem — labeled corpora, detector benchmark, latency profiling.

Everything here is offline/reproducible (seeded) and surfaced at runtime via
``GET /api/research`` so the Research Lab UI renders defensible, measured numbers
rather than asserted ones.
"""
